{
  "name": "vectora-academic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build:preview": "vite build",
    "check:release": "node scripts/check-release.mjs",
    "build": "npm run check:release && vite build",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
